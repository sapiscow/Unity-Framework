namespace Sapiscow.Framework.Systems
{
    /// <summary>
    /// Inherit this class for a main controller (system) in a module.
    /// Use this if your controller doesn't need MonoBehaviour stuffs.
    /// </summary>
    public abstract class BaseSystemNative : ISystem { }
}